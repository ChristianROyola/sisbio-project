import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sisbiopro.angular',
  appName: 'sisbio-project',
  webDir: 'dist/sisbio-project',
  bundledWebRuntime: false
};

export default config;
